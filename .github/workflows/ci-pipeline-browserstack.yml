name: Android Appium Tests BrowserStack Workflow

on:
  workflow_dispatch:

jobs:
  ubuntu-job:
    name: 'BrowserStack Test on Ubuntu'
    runs-on: ubuntu-latest
    environment: test

    steps:
      # 1️⃣ Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ BrowserStack Environment Setup
      - name: BrowserStack Env Setup
        uses: browserstack/github-actions/setup-env@master
        with:
          username: ${{ secrets.BROWSERSTACK_USERNAME }}
          access-key: ${{ secrets.BROWSERSTACK_ACCESS_KEY }}
          build-name: "GitHub Actions Run - ${{ github.run_number }}"
          project-name: "BrowserStack-App-Automate"

      # 3️⃣ BrowserStack Local Setup
      - name: BrowserStackLocal Setup
        uses: browserstack/github-actions/setup-local@master
        with:
          local-testing: start
          local-identifier: gha-${{ github.run_id }}

      # 4️⃣ Setup .NET 8
      - name: Setup .NET 8
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 8.0.x
          cache: false

      # 5️⃣ Verify SDK version
      - name: Verify .NET version
        run: |
          dotnet --version
          dotnet --list-sdks

      # 6️⃣ Build project
      - name: Build project
        run: |
          echo "🔧 Building project with .NET 8"
          dotnet build android.csproj --configuration Release

      # 7️⃣ Restore BrowserStack CLI tools
      - name: Restore .NET tools
        run: dotnet tool restore

      # 8️⃣ Run Appium tests via BrowserStack SDK
      - name: Run Appium tests
        env:
          BROWSERSTACK_USERNAME: ${{ secrets.BROWSERSTACK_USERNAME }}
          BROWSERSTACK_ACCESS_KEY: ${{ secrets.BROWSERSTACK_ACCESS_KEY }}
        run: |
          echo "🧪 Running Appium tests via BrowserStack SDK"
          dotnet browserstack-sdk bin/Release/net8.0/android.dll --logger:"console;verbosity=detailed"

      # 9️⃣ Stop BrowserStack Local
      - name: BrowserStackLocal Stop
        uses: browserstack/github-actions/setup-local@master
        with:
          local-testing: stop
